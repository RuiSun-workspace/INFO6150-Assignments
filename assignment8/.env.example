# 环境配置示例
# 复制此文件为 .env 并根据您的环境修改配置

# 服务器端口
PORT=3000

# MongoDB 连接字符串
# 本地开发环境
MONGODB_URI=mongodb://localhost:27017/assignment8

# 如果使用 MongoDB Atlas（云数据库），使用类似下面的格式：
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/assignment8?retryWrites=true&w=majority

# 注意事项：
# 1. 不要将真实的 .env 文件提交到 Git
# 2. 确保 .env 在 .gitignore 中
# 3. MongoDB 必须在启动服务器前运行
